
<chatbot_project>
│
├── app.py                  // file chính chạy ứng dụng Flask
├── chatbot_model.py        // Tệp chứa chatbot và huấn luyện mô hình
├── api_integration.py      // Tệp tích hợp API bên ngoài (ví dụ: thời tiết)
├── static/                 // Thư mục chứa tài nguyên tĩnh
│   └── css/
│       └── styles.css      // Tệp CSS cho giao diện
│   └── js/
│       └── scripts.js      // Tệp JavaScript cho tương tác
└── templates/              // Thư mục chứa template HTML
    └── index.html          // Tệp HTML chính
